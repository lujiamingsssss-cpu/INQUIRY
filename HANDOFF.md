# Chemical Trade AI Copilot 阶段性交接

> 更新时间：2026-07-28
>
> 适用对象：完全没有历史上下文的新开发会话
>
> 当前结论：阶段一至阶段五均已完成并验证。阶段五已用 BAER XP9500 官方 TDS/SDS 完成统一清单、预检、暂存重建、Top 3 golden gate、来源查看和真实回滚；本轮提交推送后 `main` 与 `origin/main` 应同步在 Git 最新 HEAD。下一会话不得重做前五阶段，未批准新的开发阶段。

## 1. 权威顺序与新会话启动步骤

文档权威顺序：

1. `PROJECT_SPEC.md`：当前唯一权威开发基线；
2. `TASK_STATUS.md`：最新验证与 Git 状态；
3. `HANDOFF.md`：帮助零上下文会话快速接手，不能覆盖前两份文件。

新会话必须先执行：

1. 完整阅读以上三份文件；
2. 在 `F:\外贸化工` 运行 `git status --short --branch`；
3. 运行 `git log -5 --oneline`，确认阶段五提交、`e688091` 和 `96126ac` 仍在历史中；
4. 如要改代码，先运行当前测试作为基线；
5. 不整理或恢复 `_excluded`，不重建前五阶段；后续资料扩展按 `docs/MATERIAL_EXPANSION_RUNBOOK.md` 操作，但任何新产品仍须人工批准，其他开发需用户另行授权。

旧任务和历史对话只能作为背景，不能覆盖 `PROJECT_SPEC.md`。尤其不要按旧交接重新建立 golden cases；它们已经完成。

## 2. 项目现在是什么

这是一个面向私人业务的“化工外贸询盘证据工作台”Demo：把中英文客户询盘拆成技术、合规、商务和物流要求，用企业自己的 TDS/SDS 给出可追溯的产品判断、证据缺口和下一步动作。

它不是：

- 公开化工商城；
- 供应商撮合或全市场比价平台；
- 通用 PDF 聊天工具；
- CRM、ERP 或自动邮件系统；
- 可处理任意产品和任意上传文件的生产平台。

竞品调研后的结论没有改变技术主线：同行已经在做海量目录、样品/询价、供应商发现和替代品匹配。本项目的差异化价值是对企业自有小型资料做可信、可核查的询盘处理。不要为了“跟上时代”复制市场平台。

## 3. 固定位置、安全边界与资料边界

- 项目：`F:\外贸化工`
- GitHub：`https://github.com/lujiamingsssss-cpu/CHEMISTRY`
- 分支：`main`
- 资料：`G:\桌面\化工`
- DeepSeek 本机配置：`G:\桌面\化工外贸api.md`
- 阶段二核心实现提交：`4acdf2c`
- 阶段三最终实现提交：`96126ac`
- 阶段四修复与截图提交：`e688091`
- 当前远端：`origin/main`；本交接提交并推送后应与本地 `main` 同步，以 Git 最新 HEAD 为准。

API 密钥只能在本机读取后放入当前进程环境变量。禁止输出、记录、提交或复制到 `.env`、源码、测试、Markdown、日志和对话。

正式批准清单当前启用：

1. `D.E.R. 331`
2. `EPON Resin 8280`
3. `BAER XP9500`

正式资料共 6 份 PDF、45 个非空物理页：

- D.E.R. 331：2009 TDS 5 页；2020 加拿大英文 SDS 13 页；
- EPON Resin 8280：2005 重新发布、页脚修订 2016 的 TDS 5 页；2022 美国英文 SDS 13 页；
- BAER XP9500：修订号 09132018 的 TDS 1 页；2018-06-28 美国英文 SDS 8 页。TDS 标注仍在产品开发中，SDS 标注仅供实验使用。

`G:\桌面\化工\_excluded` 不得索引，其中包括缺少独立 TDS 的 D.E.N. 438、D.E.R. 331 的 2001 旧 MSDS，以及错误重复的 Desmodur I 目录。

## 4. 已完成：阶段一

阶段一“资料与最小可行性验证”已完成，不得重做。以下“固定两款”描述是当时的历史范围，当前启用范围以统一清单为准：

- 盘点、哈希、文本提取和页面渲染检查；
- 纠正错误目录、隔离重复/旧版/不完整资料；
- 固定两款产品白名单，并校验 TDS/SDS 成对；
- PyMuPDF 按物理页提取；
- LangChain 页内分块且保留物理页码；
- `intfloat/multilingual-e5-small` 本地归一化向量；
- Chroma 本地持久化索引；
- 根目录逃逸防护；
- `ingest`、`query` CLI；
- 先计算新向量、成功后再替换旧索引。

基础链路：

```text
固定白名单
  → 物理页提取
  → 页内分块
  → multilingual-e5-small
  → Chroma
  → 命中片段 + 产品 + 文档类型 + 文件名 + 物理页码
```

## 5. 已完成：阶段二

阶段二“可信 RAG 核心”已完成并推送，未做 UI。

### 5.1 Golden retrieval cases

已建立 5 条固定案例：

| 案例 | 预期证据 |
|---|---|
| EPON 8280 high-solids coatings | EPON TDS 第 1 页 |
| EPON 8280 + MPDA + HDT | EPON TDS 第 3 页 |
| D.E.R. 331 汽车、船舶和防护涂料 | D.E.R. TDS 第 1 页 |
| D.E.R. 331 配方体系 HDT | D.E.R. TDS 第 2 页 |
| 200°C 连续使用 + 食品接触认证 + 长期寿命 | 必须证据不足 |

真实外贸措辞下：

- 原始 E5 + Chroma：Hit@1/3/5 = `0.25/0.25/0.25`；
- 简洁查询规划 + TDS/SDS 类型过滤后：Hit@1/3/5 = `0.75/1.00/1.00`。

只做了一个最小增强：DeepSeek 把询盘中的技术检索意图与商务噪声分开，并选择 TDS/SDS 类型。没有更换 Embedding，没有引入 BM25、RRF、重排器或 RAG 框架。

### 5.2 结构化询盘分析

`analyze` CLI 已实现：

- 需求拆分：技术、合规、商务、物流；
- 推荐或明确证据不足；
- 推荐理由；
- 关键参数和引用；
- 证据缺口、追问和下一步动作；
- DeepSeek JSON 输出为空时重试一次；
- 模型输出两次不能通过本地校验时，确定性降级为证据不足。

### 5.3 本地证据门禁

当前门禁不是单靠提示词：

- 所有引用必须存在于本次证据集合；
- `supported` 必须有引用，当前只允许技术项为 `supported`；
- 产品推荐必须有同产品的技术引用；
- 合规、库存、价格、MOQ、交期和物流不能由当前 TDS/SDS 自动确认；
- 关键参数只能来自人工核验事实集；
- 温度要求必须匹配同引用、同数值、同物性语义的核验事实；
- 温度值只允许出现在已核验的 `key_parameters`，不能在摘要、理由、缺口或追问中自由复述；
- HDT 不能当作连续使用温度，即使数值完全相同；
- 普通 `query` 不输出内部完整页文本 `page_text`。

目前唯一完成完整条件核验的原子事实是：

```text
产品：EPON Resin 8280
物性：Heat Deflection Temperature
值：156°C
固化剂：Metaphenylenediamine (MPDA)
配比：EPON Resin 8280 100 pbw : MPDA 14.4 pbw
固化制度：2 h/80°C + 2 h/150°C
测试方法：ASTM D648
来源：TDS - Hexion EPON Resin 8280 - Rev 2016.pdf，物理第 3 页
```

D.E.R. 331 的跨列表格没有完整可靠地绑定所有条件，因此没有加入事实白名单。接口予以保留，未来可以在人工核验后扩充事实集；不要为了“功能更多”绕过白名单。

### 5.4 已验证结果

- 全量自动化测试：`56 passed`；
- `pip check`：无损坏依赖；
- wheel：构建成功；
- 规划检索 4 条可回答案例 Hit@3 = `1.00`；
- MPDA 正例可以输出 EPON Resin 8280 和完整条件；
- 200°C 连续使用/食品接触认证负例无推荐、无技术参数；
- DeepSeek 偶尔会输出不合规 JSON 或重复温度，系统会重试或安全降级。这是保守行为，不得通过放宽证据规则“提高成功率”。

## 6. 已完成：阶段三 Web 界面

阶段三没有重写阶段二。`workflow.py` 只把原有“规划 → 类型过滤检索 → 完整物理页合并 → 本地证据校验”整理为 CLI 与页面共用的薄调用层。

已完成的真实界面状态：

1. 英文询盘入口：接受中英文询盘，不提供任意文件上传；
2. 证据支持结果：展示产品、四类状态、开放项、下一步、条件绑定参数、报价前待确认项和文档包状态；
3. 证据不足结果：不显示产品或未核验数值，只展示与本次需求类别对应的缺口和追问；
4. 来源查看器：只渲染白名单 PDF 的准确物理页，点击打开，支持 `50%`–`250%` 缩放、复位和点击切换；
5. 英文邮件：由已通过门禁的结构化结果确定性生成，可编辑、可复制、不连接邮箱、不自动发送。

UI 约束：

- 风格是 Editorial Brief / Mineral Ink，不做多栏工作台、密集仪表盘或商城；
- 数字使用直立字形和 tabular numerals，不使用倾斜显示；
- 真实来源必须同时显示产品、准确文件名、物理页码、日期/修订和管辖信息；放大层打开后这些信息仍需可见；
- 普通应用适用性正例只能使用保守的“文档范围内有技术回复证据”措辞；只有存在已核验的固化剂、配比、固化制度和测试方法时，邮件才可写“specified cured-system test result”；
- 四类状态、开放项、客户追问和下一步必须来自当前 `requirements` 与 `next_action`，不得用固定模板伪装成分析结果。

阶段三验证：

- 合并到 `main` 后全量自动化测试：`56 passed`；
- `pip check`：`No broken requirements found`；
- wheel：构建成功；
- 真实浏览器正例：EPON Resin 8280 + MPDA + HDT 输出完整条件、ASTM D648、TDS 物理第 3 页；
- 真实浏览器负例：200°C 连续使用 + 食品接触认证 + 长期寿命返回证据不足，无产品、无技术数值；
- 查看器缩放、复位、点击切换和邮件复制已真实验证；
- 阶段复杂度审计与独立代码复审完成，没有遗留 Critical 或 Important 问题。

## 7. 当前卡在哪里

当前没有环境、资料、权限或测试阻塞。阶段一至阶段五均已完成；项目停在**阶段五真实扩展已经验收、等待本轮提交推送**的位置，未批准下一开发阶段。

阶段顺序没有冲突：阶段四验证当时的固定两产品 Demo，阶段五在不重做前四阶段的前提下验证新增资料扩展能力；两阶段都已正式收口。

阶段四完成内容：

- 复用既有 golden cases 冻结三个演示案例，没有重建阶段二检索基准；
- 普通应用正例命中 EPON Resin 8280 TDS 物理第 1 页，邮件未夸大为固化体系试验；
- MPDA/HDT 正例命中物理第 3 页，完整展示 MPDA、100:14.4、固化制度、156°C HDT 和 ASTM D648，并明确不是连续使用温度；
- 200°C 连续使用/食品接触认证负例无产品、无技术数值、无虚构认证；
- 来源查看器元数据、缩放、复位和邮件复制完成真实浏览器回归；
- 验收发现 safe fallback 把整条询盘固定归为 technical，导致食品接触认证被显示为 `Not requested`；现已修复；
- 修复保留明确请求的 compliance、commercial、logistics 类别，同时继续拒绝模型产品、引用和数值；
- 匹配采用词/短语边界，区分法规 `21 CFR` 与贸易术语 CFR，避免 `reach`、`technical support`、`specification` 等子串误报；
- TDD 后全量 `62 passed`，两轮独立代码复审无遗留 Critical 或 Important 问题；
- 三张最终截图已提交在 `docs/demo/`。

阶段五已经完成：固定映射已迁移为统一批准清单，真实 BAER XP9500 扩展已经通过预检、暂存重建、检索、溯源、来源页查看和回滚验收。

非阻塞限制：

- 白名单只有三款产品，不能代表市场级选型；
- 三份 TDS 年代较早或带有开发状态限制；
- 当前只有一条完整人工核验参数事实；
- DeepSeek 输出存在非确定性，失败时会安全降级；
- 商务、库存、物流和认证仍需人工或未来可信数据源。
- BAER 资料尚未增加任何高风险人工核验事实；可检索不等于可以输出认证、连续使用温度或其他结构化高风险结论。

## 8. 已完成：阶段五统一资料扩展接口

完成结果：

1. `materials_catalog.json` 成为产品审批、入库、索引元数据、状态和查看器的单一事实来源；
2. 预检覆盖 PDF 可读性、路径逃逸、哈希、重复文件、产品身份、元数据和每个启用产品一份 TDS/SDS；
3. `preflight`、`rebuild`/`ingest`、`status`、`verify` 和 `rollback` 已统一；
4. 新索引在同父目录暂存构建，精确页面完整性与全部固定正向 golden 全部 Top 3 通过后才切换，并保留一个旧索引备份；
5. BAER XP9500 官方 TDS/SDS 通过普通直接下载和隔离审查，无需插件；用户人工批准后正式启用；
6. 扩展后为 3 个产品、6 份文件、45 页；5 个适用正向 case Hit@3=`1.00`；
7. BAER TDS 物理第 1 页已真实渲染；索引保存完整清单指纹，查询、分析和 UI 缓存不一致时 fail-closed；回滚到旧索引时状态明确报告清单不一致，恢复新索引后重新达到 `ready` 并通过 gate；
8. 用户要求的成功经验已写入 `docs/MATERIAL_EXPANSION_RUNBOOK.md`，但 `PROJECT_SPEC.md` 仍是唯一权威基线。

明确非目标：不做网页任意上传、无人监督抓取、账号、后台任务、公开目录、供应商平台、第二向量库、BM25/RRF/重排器或 Agent。当前数据规模下，“高效”是指一次受控重建和稳定查询，不是预建复杂增量平台。浏览器插件是必要时的资料获取辅助工具，不是产品能力。

复杂度自省结论：普通下载、单一清单和受控全量重建已经满足目标；没有创建插件、服务、后台、第二数据库、混合检索、重排器或 Agent。后续不得因“可扩展”重新增加这些层次，除非新的固定失败案例和用户授权同时成立。

## 9. 绝对不要再踩的坑

### 9.1 不要重做已完成阶段

阶段一至阶段五已经完成。不要重新整理既有资料、重新选择向量库、重建既有 golden cases、重做 UI 或扩展接口，或把已完成工作描述成待办。

### 9.2 不要因竞品调研把 Demo 变成市场平台

商城、供应商网络、实时价格、撮合和海量产品目录是别人的产品路径，不是当前项目边界。

### 9.3 不要把问题复杂化

当前三款产品已经用 E5 + Chroma + 一次查询规划达到 Hit@3 = 1.00。没有固定失败案例时，禁止增加 BGE-M3、BM25、RRF、多查询、重排器、Agent、工作流编排、知识图谱或生产数据库。

### 9.4 不要把 top-k 未命中写成资料不存在

top-k 只代表排序结果。当前实现会在排序后把所选文档类型的完整页集合用于核验。没有完整核验时只能说“当前检索证据不足”。

### 9.5 不要让商务噪声污染技术检索

MOQ、CFR、库存、目的港和交期曾显著拉低召回。查询规划只提取可在 TDS/SDS 搜索的技术/安全意图，不直接回答询盘。

### 9.6 不要退回整页单向量，也不要只给模型命中小块

整页可能超过 E5 上下文，页尾参数会丢失；只给模型小块又可能拆散表格条件。当前设计是“小块排序、完整物理页核验、准确页码溯源”。

### 9.7 不要自行配对 PDF 表格列

模型曾把 24 h/23°C 与 MPDA 列错误绑定。EPON 的 156°C 事实由人工核验并规范化；D.E.R. 跨列表格未核验，因此不自动输出数值。

### 9.8 不要把 HDT/Tg 当树脂本体耐温或连续使用温度

固化体系性能必须与固化剂、配比、固化制度和测试方法绑定。HDT 与连续使用温度是不同物性，同为 156°C 也不能互换。

### 9.9 真实引用不等于主张真实

模型曾用真实 EPON 第 3 页包装虚构黏度、库存和认证。引用身份校验不够，所以关键参数采用人工事实白名单，非技术项不得自动标记为 supported。

### 9.10 不要在自由叙述中重复温度

模型会把合法 HDT 在摘要中改写成“可长期连续使用”。温度只放在经过核验的结构化参数中，不为了文案好看重复到摘要和推荐理由。

### 9.11 不要只靠反复修改提示词

空 JSON、截断、错误表格配对和同义词绕过都发生过。高风险规则必须由本地校验和回归测试保证；提示词只负责引导。

### 9.12 不要把 SDS 温度当应用性能

闪点、分解温度、储存温度和操作温度不能替代涂层连续使用温度。SDS 只用于安全、运输、操作和管辖信息。

### 9.13 不要泄露内部完整页文本

`page_text` 用于内部证据核验，普通 `query` JSON 只输出命中块和溯源元数据。

### 9.14 不要重新开放任意产品或任意上传

CLI 当前不允许 `--product` 覆盖。阶段五可以通过统一批准清单加入经过审核的新产品，但不能接受任意命令行覆盖、网页上传或根目录外路径；清单本身就是新的白名单。

### 9.15 不要先清空索引再计算向量

当前至少保持“先成功计算新向量，再替换旧记录”。阶段五必须进一步改为独立临时索引完成构建与回归后再原子切换，并保留可回滚旧索引；禁止直接向正式集合边删边写。

### 9.16 不要为了精简删除正在使用或可验证扩展的接口

`doc_types` 过滤、完整页读取、golden evaluator、结构化 schema 和事实集扩展点都有当前用途或清晰升级路径，应保留。边界控制的含义是“不启用、不预建空平台”，不是把未来可能使用的可靠接口全部删除。

### 9.17 不要隐瞒资料年代、地区或模型降级

旧 TDS、加拿大/美国 SDS 和模型 fail-closed 都必须如实展示。安全降级不是成功推荐，也不是系统故障。

### 9.18 不要创建更多管理文档

只维护 `PROJECT_SPEC.md`、`TASK_STATUS.md` 和本文件。`docs/MATERIAL_EXPANSION_RUNBOOK.md` 是用户在阶段五首次扩展成功后明确要求的操作手册例外，不是新的权威基线；除非用户再次明确要求，不创建其他计划、调研、决策、日志或交接 Markdown。

### 9.19 不要让邮件模板超出实际证据类型

阶段三复审曾发现：只证明“high-solids coatings 应用”的支持结果也会套用“固化体系试验已支持”的邮件句式。现已修复。以后必须按真实核验事实选择措辞；有真实引用不代表可以补写另一类技术主张。

### 9.20 不要用固定 UI 文案覆盖真实分析结果

四类状态、证据缺口、追问和下一步曾被固定模板统一展示，导致不同询盘看起来得到相同结论。现已改为按结构化类别和 `next_action` 保守映射。不要为了页面整齐恢复成“一套话术走天下”。

### 9.21 不要让放大层遮住来源身份

来源缩略图和放大层都必须持续显示产品、文件名、物理页码、日期/修订和管辖信息。当前元数据来自四份批准文件的固定可信映射；阶段五必须迁移到统一批准清单，仍然不能由模型自由生成。

### 9.22 不要用环境偶然性掩盖工作树导入错误

隔离工作树测试曾错误导入主工作区的旧 editable package，导致新模块在全量测试收集时缺失。`pyproject.toml` 已用 `pythonpath = ["src"]` 固定当前仓库源码优先。不要靠临时 `PYTHONPATH` 或重新安装来掩盖测试入口问题。

### 9.23 不要为无关文件监视告警安装大型依赖

Streamlit 文件监视器枚举 `transformers` 懒加载模块时曾触发未使用的 `torchvision` 导入告警。根因不是页面缺依赖，现已通过 `[server] fileWatcherType = "none"` 关闭开发热监视。不要安装 torchvision，也不要把该告警误判为模型或页面功能失败。

### 9.24 不要为模型延迟预建后台架构

真实两段 DeepSeek 调用可能需要约 1–3 分钟。当前 Demo 有加载状态并保持 fail-closed；没有固定验收案例证明必须异步化之前，禁止新增队列、任务系统、缓存服务、轮询 API 或第二套模型链路。

### 9.25 不要把阶段五写回未实现或夸大为任意扩展平台

统一资料清单、预检、临时构建、目录切换、状态一致性和回滚已经用 BAER XP9500 真实验证。不要把这些能力写回待办，也不要把受控、逐产品人工批准的扩展流程夸大为任意上传、自动采集或市场级产品平台。

### 9.26 不要用简单子串判断询盘类别

阶段四首次修复把 `reach`、`port`、`cif`、`cfr` 当普通子串，独立复审发现会把 “reach 200 C”、`technical support`、`specification` 和法规 `21 CFR` 误判成其他类别。现已改用词/短语边界与 CFR 上下文区分。不要退回裸 `in` 匹配。

### 9.27 不要在工作树验收时导入主工作区旧包

阶段四曾在隔离工作树启动 Streamlit，但 Python 实际解析到主工作区 editable package，导致浏览器仍显示旧行为。测试入口已经由 `pythonpath = ["src"]` 固定；真实工作树运行也必须先核对 `module.__file__` 指向当前工作树。显式路径只用于验证隔离来源，不能替代仓库测试配置。

### 9.28 不要把“可扩展”做成原地增量堆积

当前语料小，全量受控重建更简单可靠。禁止为了表面效率直接向正式 Chroma 追加、留下旧版本、重复哈希或半成品；先构建、验证，再切换。

### 9.29 新资料不等于新事实已经核验

新 PDF 通过预检并进入向量库，只代表它可以被检索。HDT、Tg、配比、固化制度、认证和其他高风险主张仍须人工核验并加入事实白名单，不能从新文档自动升级为结构化“supported”。

### 9.30 不要让统一资料目录分裂成多套配置

产品审批、入库文件、索引元数据和 PDF 查看器必须读取同一清单。不要保留一套 `APPROVED_PRODUCTS`、一套入库配置、另一套查看器日期/管辖映射，否则新增资料必然出现漂移。

### 9.31 不要把浏览器插件变成爬虫或信任边界捷径

用户允许在阶段五客观需要时编写浏览器插件自行下载公开资料，但这不是绕过访问控制或自动扩充白名单的授权。普通下载可用时不写插件；需要插件时也只允许人工触发、批准域名、选定文件和隔离输出。插件不得处理凭据、破解验证码、绕过账号/付费墙/许可，不得把下载文件直接送入正式索引。

## 10. 常用验证命令

```powershell
cd "F:\外贸化工"

git status --short --branch

.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m build --wheel

.\.venv\Scripts\python.exe -m chemical_trade_copilot.cli preflight `
  --materials-root "G:\桌面\化工" --catalog materials_catalog.json
.\.venv\Scripts\python.exe -m chemical_trade_copilot.cli status `
  --catalog materials_catalog.json --database .chroma
.\.venv\Scripts\python.exe -m chemical_trade_copilot.cli verify `
  --catalog materials_catalog.json --database .chroma `
  --golden-cases tests\fixtures\golden_retrieval_cases.json

.\.venv\Scripts\python.exe -m streamlit run src\chemical_trade_copilot\streamlit_app.py

.\.venv\Scripts\python.exe -m chemical_trade_copilot query `
  "EPON Resin 8280 heat deflection temperature MPDA cure schedule" `
  --database .chroma `
  --limit 3
```

`analyze` 需要当前进程中的 `DEEPSEEK_API_KEY`。密钥从本机配置安全加载，不要把加载后的值打印到终端。

构建验证会生成 `dist`；不要把 wheel 当源码提交。`.chroma`、本地 PDF、模型缓存、`.venv`、`.env` 和浏览器日志也不得进入 Git。

## 11. 交接完成标准

新会话至少要记住：

1. `PROJECT_SPEC.md` 是唯一权威基线；
2. 阶段一至阶段五都已完成，不要重做；
3. 当前没有阻塞，也没有已批准的下一开发阶段；
4. 这是三款产品的私人 Demo，不是商城或生产平台；
5. 不要复杂化，增强必须由固定失败案例驱动；
6. 检索没命中不等于资料不存在；
7. 真实引用不等于真实主张，关键参数必须经过事实门禁；
8. HDT/Tg 不能当连续使用温度；
9. 后续资料扩展按运行手册执行，仍只允许统一批准清单、预检、安全重建、目录切换、回滚和回归门禁；
10. 每阶段测试、动态验证、文档、提交和推送后立即停止。
